namespace SqlInitializer.Sql
{
    public static class Initializer
    {
        public static bool InitializeDatabase(string server, string userName, string password)
        {
            return true;
        }
    }
}